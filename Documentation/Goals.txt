First goal, set up a proper unit system to take in values accordingly.
System is now in place, now need to pull out values, and then update em accordingly
The next step is to work on the mathematics behind the upgrades
The mathematics is incomplete, buy amount is not properly set, finish that then can work on next step.
Currently working on base side first, and then work can be done on the adder side.



The mathematics are technically done now, just need to now account for effects and have them apply every turn and other end of turn effects
#make minimally viable product of the cycles. So energy buyable, and effects of the purchases should be done, as well

Discovered bug on upgrade buying system, patched up, resolved native bug of big system, will need to comb over later on. Regardless patch works

There is a crashing bug that happens if the button is clicked to quickly. Resolved

Now work on energy system to be bought and consumed by turns.

Energy is now buyable and consumed by turns, values do need to be played around with, but definitely works now.


The final pieces are almost in place,
now the reset loop and additional factors to be applied afterwards has to be done to get working.

Bugs are discovered, currently the value of upgrades DOES NOT reflect what the actual math shows
Therefore, there is some calculations in need of fixing.
First calculation is fixed, the levels now reflect actual cost appropriately, but buy max and buy amounts are not accurate.
all associated problems are now fixed, 
back to work on reset functions.
The reset function works, now tabs need to be developed
Tab is now unlockable.


#note, a factor in which that must be considered is that x cannot equal 1, it must be subtracted by 1 and thus
 a specific function is needed to be in place just in case for one being used.

Here is the basic math for each step.
for each upgrade do these steps
get base, then add adder to it.
then multiply summed result by multiplier scaler.
This is the formula
bs_1 =(bs_0 +ad) ms


This is the formula accordingly when level is put into place

(bs*(ms^level)) +(ad*level*ms^(level*(level+1)/2))
 
if  level is 1
ms*ad
if level is 2
ms*ad*ms +ms*ad
if level is 3
ms*ad*ms + ms*ad + ms*ad*ms*ms
if level is 4
ms*ad*ms + ms*ad + ms*ad*ms*ms + ms*ms*ms*ms*ad

simplification in place
ad*level * ms^(level(level+1)/2)

mathematics are as follows for base side
bs = base
ms = multiplier scaler
x = x terms
(bs * (ms^(x+1) -1) /ms-1)

adder side
ad = adder
((ad*ms) / ((ms-1)^2)) * (ms ^(x+1) -1 - (x+1)(ms-1))



(1+1)*1.2
1.2 + 1.2
(2.4 + 1)*1.2
3.4*1.2 = 4.08
(1.2 + 1.2 + 1)*1.2
1.44 + 1.44 + 1.2
2.88 +1.2 = 4.08

1.2 ^2
+ adder*ms^x + adder*ms^x-1 ..... until x-y = 0
